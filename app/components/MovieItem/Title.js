import styled from 'styled-components';

const Title = styled.h3`
  margin: 0;
  padding: 0;
  padding-top: 0.5rem;

  text-overflow: ellipsis;
  white-space: nowrap;
  overflow: hidden;

  font-size: 0.9rem;
`;

export default Title;
